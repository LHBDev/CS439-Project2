#include <bitmap.h>
//Put Desired methods, put desired includes, maybe some struct definitions
//such

//Swap table methods
void swap_init (void);
void * swap_in (uint8_t *, int);
void swap_out (uint8_t *);
